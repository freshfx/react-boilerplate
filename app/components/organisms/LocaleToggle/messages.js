/*
 * LocaleToggle Messages
 *
 * This contains all the text for the LanguageToggle component.
 */
import {defineMessages} from 'react-intl'

const scope = 'boilerplate.organisms.LocaleToggle'

export default defineMessages({
  de: {
    defaultMessage: 'de',
    id: `${scope}.de`
  },
  en: {
    defaultMessage: 'en',
    id: `${scope}.en`
  }
})
